# 明日の作業手順書 — 配線・組立

Auto-Trash Navigator / 差動2輪版
電源 3S LiPo 5000mAh 80C / MD10C ×2 / ESP32

---

## 0. この日のゴール

**車体を持ち上げた状態で `v 0.2 0` と `v 0 1.0` が正しく動くところまで。**
床に降ろすのは全部の符号が合ってから。

所要は正味4〜6時間。焦って通電すると壊すので、各段階のチェックを飛ばさないこと。

---

## 1. 準備

### 工具
テスター、はんだごて、ワイヤストリッパ、圧着ペンチ、ノギス、六角レンチ、
プラスドライバ(#1, #2)、ニッパ、熱収縮チューブ、結束バンド

### 部品(手元にあるもの)
- OSOYOO メカナムキット(ホイール4、モーター4、カップリング4)
- MD10C ×2
- ESP32 DevKitC
- LiPo 3S 5000mAh 80C + XT60
- エーモン 1244 リレー(4極 12V 20A)
- 22mm キノコスイッチ(NC)
- ブレードヒューズ + ホルダー
- フェライトコア
- 印刷済みプレート類

### 買い足しが要るもの(未着ならこの日は飛ばせる)
- セラコン 0.1µF ×12 → **なくても回るが、あるほうがいい**
- 1N4007 → E-stop 回路に必要。**なければ E-stop は後回し**
- 電解コンデンサ 1000µF → アーム接続時までは不要

---

## 2. 最初に測る(15分)

通電前に確定させること。

| # | 測るもの | 方法 | 判断 |
|---|---|---|---|
| M1 | **カップリング外径** | ノギス | 16mm以下ならOK。18mm超ならブラケット刷り直し |
| M2 | **カップリングの内側の深さ** | ノギス | 軸13mmが入るか |
| M3 | MD10C の端子配置 | 目視 | 下記と一致するか |

### M1 が重要な理由

モーター軸は、ブラケットのフェースプレート(厚6mm)の外に **7mmしか出ない**。
取付ネジの頭が半径9mmに出るので、カップリング外径18mm超だとネジ頭に当たる。

当たる場合の対処:
- フェースプレートを削る(現物合わせ、ヤスリで3mmに)
- または `motor_bracket` 4個を刷り直し(51g×4、約40分)

---

## 3. MD10C の端子(現物で確認)

MD10C V3.x の想定。基板のシルクを必ず確認すること。

```
┌─────────────────────────┐
│  [ MOTOR ]   [ POWER ]  │   ← 太いネジ端子
│   A    B      +    −    │
│                         │
│  ┌───────┐              │
│  │ PWM   │ ← 信号ヘッダ  │
│  │ DIR   │              │
│  │ GND   │              │
│  └───────┘              │
│                         │
│  [TEST A] [TEST B]      │ ← 押しボタン
└─────────────────────────┘
```

| 端子 | 繋ぐ先 |
|---|---|
| POWER + | リレー出力 → ヒューズ10A |
| POWER − | バッテリー GND |
| MOTOR A / B | モーター2個を並列 |
| PWM | ESP32 GPIO |
| DIR | ESP32 GPIO |
| GND | **ESP32 の GND(必須)** |

**TESTボタンが便利。** ESP32 を繋ぐ前に、電源だけ入れてボタンを押せばモーターが回る。
配線の正しさをここで確認できる。

---

## 4. 第1段階 — モーター1個を回す(45分)

ブレッドボードで組む。**まだ並列にしない。**

### 4-1. 配線

```
[LiPo +] ─ XT60 ─ [ヒューズ10A] ─ [MD10C POWER +]
[LiPo −] ──────────────────────── [MD10C POWER −]
                                        │
                                   [ESP32 GND] ★共通GND必須

[MD10C MOTOR A] ── モーター 黒(VM)
[MD10C MOTOR B] ── モーター 赤(GND)
     ※色が逆表記。コネクタのラベルで確認すること

[MD10C PWM] ── ESP32 GPIO 13
[MD10C DIR] ── ESP32 GPIO 27
[MD10C GND] ── ESP32 GND

モーター 白(ホール電源) ── ESP32 3V3
モーター 黄(ホールGND)  ── ESP32 GND
モーター オレンジ(S1)   ── ESP32 GPIO 16
モーター 緑(S2)         ── ESP32 GPIO 17
```

**エンコーダは3.3Vで確認済み(実測1.4V平均)なので、分圧は不要。**

ESP32 は PC から USB 給電。

### 4-2. 通電前チェック

| 項目 | 方法 | 合格 |
|---|---|---|
| 短絡 | POWER + と − 間を導通モード | **導通しない** |
| 極性 | XT60 の赤が + か | 合っている |
| 共通GND | MD10C GND と ESP32 GND が繋がっているか | 繋がっている |

### 4-3. 通電と確認

`atn_base_esp32_diff.ino` を書き込んでから電源を入れる。

```
l 60      → 左ペア(いまはモーター1個)が回る
s         → 停止
l -60     → 逆回転
e         → エンコーダのカウントを確認
```

| 症状 | 原因 |
|---|---|
| 回らない | 共通GND忘れ(最多)。または PWM/DIR の挿し間違い |
| カウントが増えない | S1/S2 が逆。GPIO 16 と 17 を入れ替える |
| カウントが飛ぶ | ノイズ。セラコンを付ける |

### 4-4. ★CPR の実測(最重要)

ソフト側のオドメトリがこの値で決まる。

1. ホイールを軸に付ける(仮止めでいい)
2. ホイールに油性ペンで印を付ける
3. `z` でクリア
4. **手でゆっくり10回転させる**
5. `e` でカウントを読む
6. カウント ÷ 10 = 1回転のカウント数

**3300 は仮定値。** 実測値を `CPR` に書き換えて再書き込みすること。
測った値をソフト側チャットにも共有する。

---

## 5. 第2段階 — 並列化(30分)

### 5-1. 配線

MD10C ① の MOTOR A/B に、モーター2個を並列で繋ぐ。

```
[MOTOR A] ─┬─ モーター FL 黒
           └─ モーター RL 黒
[MOTOR B] ─┬─ モーター FL 赤
           └─ モーター RL 赤
```

**この時点では極性が合っているか分からない。**

### 5-2. ★極性の確認(必ず低dutyで)

```
l 50      ← 必ず50から。いきなり180にしない
```

| 結果 | 対処 |
|---|---|
| 2個が同じ向きに回る | ✅ そのまま |
| 逆向きに回る | ❌ **すぐ `s` で停止。** 片方の黒赤を入れ替える |
| 両方止まって唸る | ❌ 逆向き。押し合っている。すぐ停止 |

逆向きのまま回すと、押し合って電流だけが流れ、**ドライバとモーターが焼ける**。
50 duty なら数秒は耐えるが、それ以上は危険。

### 5-3. 電流の確認

正しく並列になったら、電流計で確認する。

| 状態 | 期待値 |
|---|---|
| 無負荷 | 0.4A 前後(2個ぶん) |
| 手で軽く負荷 | 1〜2A |
| 両輪ストール(1秒以内) | 9A 前後 |

**ヒューズは10A。** 15A を入れないこと(ドライバが先に飛ぶ)。

---

## 6. 第3段階 — 4輪 + 2ドライバ(60分)

### 6-1. ドライバの割り当て

| ドライバ | モーター | PWM | DIR |
|---|---|---|---|
| **MD10C ①(左)** | FL + RL | GPIO 13 | GPIO 27 |
| **MD10C ②(右)** | FR + RR | GPIO 14 | GPIO 32 |

### 6-2. エンコーダ4本

| モーター | S1 | S2 |
|---|---|---|
| FL | GPIO 16 | GPIO 17 |
| FR | GPIO 18 | GPIO 19 |
| RL | GPIO 21 | GPIO 22 |
| RR | GPIO 23 | GPIO 5 |

ホール電源(白)は4本まとめて ESP32 の 3V3、GND(黄)も4本まとめて GND。

**3V3 の電流に注意。** ホールIC 4個で 40〜80mA 程度。ESP32 の 3V3 レギュレータは
500mA 程度出せるので足りるが、不安定なら外部の 3.3V を用意する。

### 6-3. 左右の回転方向

```
l 60      → 左2輪が前進方向に回るか
r 60      → 右2輪が前進方向に回るか
```

**右が逆なら**、コードの `DIR_SIGN[1]` を `+1` に変える(初期値は `-1`)。
配線を入れ替えるより、コードで直すほうが確実。

### 6-4. エンコーダの符号

```
z         → クリア
l 60      → 3秒回す
s
e         → FL と RL が両方 + で増えているか
```

減っていたら `ENC_SIGN[]` の該当を反転。

---

## 7. 第4段階 — 走行試験(30分)

**車体を持ち上げた状態で行う。** 台の上に載せて車輪を浮かせる。

| # | コマンド | 期待 |
|---|---|---|
| G1 | `v 0.2 0` | 4輪すべて前進方向 |
| G2 | `v 0 1.0` | 左が後、右が前(反時計回り) |
| G3 | `v 0 -1.0` | 左が前、右が後 |
| G4 | `s` → `o` | オドメトリが妥当な値 |

### 床での実測

符号が全部合ったら床に降ろす。

```
z                    → クリア
v 0.15 0             → 前進
（約7秒後）s
o                    → odom x を読む
```

**巻尺で実測した距離と `odom x` を比較。**
誤差が5%以内なら CPR は正しい。大きくずれるなら CPR を測り直す。

旋回も同様に:
```
z
v 0 0.5
（約6.3秒 = 半回転）s
o                    → th が 3.14 前後になるか
```

**ここで th がずれるなら `HALF_SEP` を疑う。**
0.300 が正しい値(0.180 ではない)。

---

## 8. E-stop 回路(部品が揃っていれば)

### 8-1. 回路

```
[LiPo +] ─ XT60 ─ 主スイッチ ─ [20A ヒューズ] ─┬─────────────┐
                                              │             │
                                        リレー接点30     リレーコイル85
                                              │             │
                                              │        キノコSW(NC)
                                              │             │
                                        リレー接点87    リレーコイル86
                                              │             │
                                    ┌─────────┴──┐         GND
                                 10A│         10A│
                              MD10C①        MD10C②
```

エーモン 1244 の端子番号:
- **30** = 常時電源入力
- **87** = 出力(コイル通電時に 30 と繋がる)
- **85 / 86** = コイル

### 8-2. フライバックダイオード

**1N4007 をコイル(85-86)に並列。** 向きは:

```
85(＋側) ──┬── カソード(帯のある側)
           │
          1N4007
           │
86(−側) ──┴── アノード
```

逆に付けると電源短絡になるので、テスターのダイオードモードで向きを確認してから。

### 8-3. 動作確認

| 操作 | 期待 |
|---|---|
| キノコを引き上げる(通常) | カチッと鳴り、モーターが動く |
| キノコを押す | 即座にモーターが止まる |
| キノコを押したまま `l 60` | 反応しない |

**ESP32 とミニPC はリレーの外側**なので、E-stop を押しても生きている。
これは意図した設計。

---

## 9. ノイズ対策(時間があれば)

セラコンがあるなら、**モーターを組み付ける前に**やる。後からだと手が入らない。

モーター1個につき3個:

```
モーター端子 A ──┬── 0.1µF ──┬── モーター端子 B
                 │            │
              0.1µF        0.1µF
                 │            │
                 └─ 金属筐体 ─┘
```

筐体側は、モーターのケースにはんだ付けするか、ケースに巻いた銅箔テープに落とす。

モーター線はツイストして、フェライトコア(Φ5〜7)を通す。

---

## 10. 通電前の最終チェック(毎回やる)

| # | 項目 | 合格 |
|---|---|---|
| 1 | POWER + と − の導通 | なし |
| 2 | 各ヒューズが入っているか | 主20A、MD10C 各10A |
| 3 | 共通GND | MD10C GND ↔ ESP32 GND |
| 4 | 極性 | XT60 の赤が + |
| 5 | 並列モーターの向き | 低dutyで確認済み |
| 6 | ESP32 の 3V3 に 5V が入っていないか | 入っていない |

---

## 11. 明日ソフト側に渡す数値

| 項目 | 値 |
|---|---|
| CPR(1回転カウント) | ____ ← **実測必須** |
| wheel_separation | **0.600** |
| wheel_radius | 0.040 |
| DIR_SIGN | ____ / ____ |
| ENC_SIGN ×4 | ____ ____ ____ ____ |
| odom 直進 1m の実測誤差 | ____ m |
| odom 半回転の実測誤差 | ____ rad |

---

## 12. トラブル早見表

| 症状 | 原因の順 |
|---|---|
| モーターが動かない | ①共通GND ②PWM/DIRの配線 ③ヒューズ切れ ④duty が0 |
| 片側だけ動かない | ①ドライバの信号線 ②並列の片方が断線 |
| 2個が押し合う | 並列の極性。片方を入れ替える |
| カウントが増えない | S1/S2 の入れ替え、または 3V3 が来ていない |
| カウントが飛ぶ | ノイズ。セラコンとフェライト |
| 走ると USB が切断される | モーターのノイズ。フェライト必須 |
| odom が実測の1.67倍 | HALF_SEP が 0.180 になっている。0.300 が正しい |
| 旋回が指令の6割 | 同上 |

---

## 13. やらないこと

- **床に降ろしての走行は、全部の符号が合ってから。** 暴走すると机から落ちる
- **duty 180 をいきなり出さない。** 必ず50から
- **Nav2 を繋ぐのはこの日はやらない。** 開ループ真値試験が先
- **アームは繋がない。** サーボ電源が未配線
